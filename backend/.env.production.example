# Production Environment Variables for Render Deployment
# Copy these to Render's environment variables section

# Server Configuration
NODE_ENV=production
PORT=3000

# ML Service - Replace with your actual Render ML service URL
ML_SERVICE_URL=https://credibility-analyzer-ml.onrender.com
ML_SERVICE_TIMEOUT_MS=30000

# CORS - Replace with your actual Render frontend URL
CORS_ORIGINS=https://credibility-analyzer-frontend.onrender.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=100

# MongoDB - Use MongoDB Atlas connection string
# Get from: https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/credibility_prod?retryWrites=true&w=majority
MONGODB_POOL_SIZE=10
MONGODB_READ_PREFERENCE=primaryPreferred

# Redis - Use Render Redis connection string
# Get from Render Redis service dashboard
REDIS_URI=redis://red-xxxxx:6379
REDIS_TLS=false
REDIS_CACHE_TTL=3600

# Sentry Error Tracking (optional)
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
SENTRY_TRACES_SAMPLE_RATE=0.1

# Prometheus Metrics
PROMETHEUS_ENABLED=true
